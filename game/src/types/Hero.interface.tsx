import { RaceName } from ".";

export type Hero = {
    name: String,
    race: RaceName,
    hpMin: Number,
    hpMax: Number,
    mpMin: Number,
    mpMax: Number,
    dmMin: Number,
    dmMax: Number,
    df: Number
}
